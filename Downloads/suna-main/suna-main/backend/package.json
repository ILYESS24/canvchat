{
  "name": "kortix-backend",
  "version": "1.0.0",
  "description": "Kortix Backend API for Render",
  "main": "api.py",
  "scripts": {
    "dev": "uvicorn api:app --reload",
    "start": "uvicorn api:app --host 0.0.0.0 --port $PORT",
    "build": "echo 'Python build handled by Render'",
    "test": "python -m pytest"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
